#### 修改代码
我已经修改部分代码，更改变量名，之后请基于目前版本回答

#### 添加注释
```
请为下面的 Python / PyTorch 代码写或重写注释，不允许修改代码内容，严格模仿 NumPy docstring（numpydoc）风格，并保持与实现一致。只输出结果，不要解释，不要额外文字。
要求：
1) 使用英语注释。
2) 函数、类、类的__init__、以及类的方法都要有三引号 docstring，放在定义后的第一行。
3) 类 docstring：只用 1 句话概述用途（不要展开细节）。
4) 函数、类的__init__和类的方法 docstring 的结构固定：
   - 1–3 句说明做什么；必要时说明张量形状/坐标含义或关键数学定义。
   - 然后写：
     Parameters
     ----------
     <param_name> : <type>
         <说明：语义、shape、约束、默认值或取值范围（如适用）>
     ...

     Returns
     -------
     <return_name> : <type>
         <说明：返回对象的语义与形状>
     ...
4) 参数与返回值使用 “name : type” 格式；说明行缩进 4 个空格。
5) 在函数体内部只写少量行内注释（#），用于标记关键阶段或意图，每条尽量短，不写长段落。
6) 类中的类初始化调用使用（##）写少量注释，写在定义的前一行
7) 术语保持一致，不要同义词反复替换。
8) 注释必须与代码逻辑完全一致；如果代码与旧注释冲突，以代码为准修正注释。

额外要求：无

具体代码如下：

```

#### 提供测试
```
请为给定的 Python函数或类编写测试代码，用于验证核心行为是否正确。测试风格与结构必须严格遵守以下要求，且不允许修改被测代码本身。

1) 总体目标：
- 使用英语。
- 测试应以“正常使用”为准（如pytorch模块只使用forward函数），不允许直接调用内部 helper 方法，也不要直接读写内部私有逻辑，除非特别注明测试要求。
- 测试覆盖不同输入条件下的行为变化，并验证代码逻辑是否正确。
- 只输出test函数和if __name__ == "__main__":以及之后内容，不重复输出被测函数，不要解释，不要额外文字。

2) 代码结构要求：
- 为被测对象创建一个 test_<Name>() 函数，所有测试逻辑都放在这个函数里。
- 在文件末尾使用：
  if __name__ == "__main__":
      test_<Name>()
  作为入口。
- 每个 test case 前必须写一行或多行以 # 开头的注释，用于说明该 test case 的目的，不允许在其他地方写任何注释。
- 使用 assert 进行检查；断言失败应能直接定位问题（必要时用 assert ... , "message" 提供简短错误信息）。
- 打印一行简短的通过信息（例如 print("test_<Name>: PASSED")）。
- 若需要新增 import（除了用户已给出的 import 之外），不要放在文件顶部，而是把新增 import 放在 if __name__ == "__main__": 之后的前几行，再调用 test_<Name>()。
  
3) 具体测试内容要求：
- 如果测试设计device，则同时提供cpu、cuda和mps上的测试。

具体代码如下：
  
```